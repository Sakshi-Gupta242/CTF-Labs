# Backend port (mapped in docker-compose.yml)
PORT=4002

# CTF flag — exposed via document.cookie on the admin panel
# (set in frontend/app.js; override here for documentation only)
FLAG=FLAG{stored_xss_master}
